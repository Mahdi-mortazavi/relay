<?xml version="1.0" encoding="utf-8"?>
<!--
    The themed-icon layer (Android 13+). Android tints this from the wallpaper
    palette and uses only its alpha, so the shadow and the highlight would each
    fill in solid and thicken the mark. This is the same arc with only its face.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:pathData="M29.5,67 C29.5,33 78.5,33 78.5,67"
        android:strokeColor="#FFFFFFFF"
        android:strokeWidth="11.4"
        android:strokeLineCap="round" />
</vector>
